name: CI
on:
  merge_group: {}
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

permissions:
  actions: read
  code-quality: write
  contents: read
  packages: read

jobs:
  validate:
    name: Node.js ${{ matrix.node-version }}
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        node-version:
          - 26
          - 24
          - 22
    steps:
      - name: ⬇️ Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
      - name: ⚙️ Setup
        uses: w5s/actions/setup@a3ec6110cc4b9f333bd8589c68ed919c3ae260b9 # main
        with:
          node-version: ${{ matrix.node-version }}
      - name: 🔨 Build
        uses: w5s/actions/task@a3ec6110cc4b9f333bd8589c68ed919c3ae260b9 # main
        with:
          task: build
      - name: 🔍 Validate
        uses: w5s/actions/task@a3ec6110cc4b9f333bd8589c68ed919c3ae260b9 # main
        with:
          task: validate
