name: CI
on:
  merge_group: {}
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
jobs:
  build:
    name: Node.js ${{ matrix.node-version }}
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        node-version:
          - 24
          - 22
          - 20
    steps:
      - name: ⬇️ Checkout
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6
      - name: ⚙️ Setup
        uses: w5s/actions/setup@17696f32d9b56bc2c1616f53d1a3fb08fde6983b # main
        with:
          node-version: ${{ matrix.node-version }}
      - name: 🔨 Build
        uses: w5s/actions/task@17696f32d9b56bc2c1616f53d1a3fb08fde6983b # main
        with:
          task: build
      - name: 🔍 Validate
        uses: w5s/actions/task@17696f32d9b56bc2c1616f53d1a3fb08fde6983b # main
        with:
          task: validate
