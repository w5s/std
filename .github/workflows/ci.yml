name: CI
on:
  merge_group: {}
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
jobs:
  build:
    name: Node.js ${{ matrix.node-version }}
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        node-version:
          - 24
          - 22
          - 20
    steps:
      - name: ⬇️ Checkout
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6
      - name: ⚙️ Setup
        uses: w5s/actions/setup@e378539a49d5fea3b74262ca67adf143e82602ca # main
        with:
          node-version: ${{ matrix.node-version }}
      - name: 🔨 Build
        uses: w5s/actions/task@e378539a49d5fea3b74262ca67adf143e82602ca # main
        with:
          task: build
      - name: 🔍 Validate
        uses: w5s/actions/task@e378539a49d5fea3b74262ca67adf143e82602ca # main
        with:
          task: validate
