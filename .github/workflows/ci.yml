name: CI
on:
  merge_group: {}
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
jobs:
  build:
    name: Node.js ${{ matrix.node-version }}
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        node-version:
          - 26
          - 24
          - 22
    steps:
      - name: ⬇️ Checkout
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6
      - name: ⚙️ Setup
        uses: w5s/actions/setup@cfb4ccbef12923f6af91fec2ddcf3d66b73b5855 # main
        with:
          node-version: ${{ matrix.node-version }}
      - name: 🔨 Build
        uses: w5s/actions/task@cfb4ccbef12923f6af91fec2ddcf3d66b73b5855 # main
        with:
          task: build
      - name: 🔍 Validate
        uses: w5s/actions/task@cfb4ccbef12923f6af91fec2ddcf3d66b73b5855 # main
        with:
          task: validate
